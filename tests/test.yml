---
- hosts: localhost
  remote_user: root
  connection: local
  vars_files:
    - ../defaults/main.yml
    - ../vars/vars-MacOSX.yml
  vars:
    install_color_schemes: no
  tasks:
    - include: ../tasks/main.yml